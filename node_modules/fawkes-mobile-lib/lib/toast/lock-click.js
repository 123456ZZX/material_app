"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.lockClick = lockClick;
var lockCount = 0;
function lockClick(lock) {
  if (lock) {
    if (!lockCount) {
      document.body.classList.add('fm-toast--unclickable');
    }
    lockCount++;
  } else {
    lockCount--;
    if (!lockCount) {
      document.body.classList.remove('fm-toast--unclickable');
    }
  }
}