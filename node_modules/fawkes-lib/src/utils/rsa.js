import JsEncrypt from 'jsencrypt'
export function getRSApassword(key, password) {
  const jse = new JsEncrypt()
  jse.setPublicKey('`-----BEGIN PUBLIC KEY----- \n ' + key + ' \n -----END PUBLIC KEY-----`')
  const encrypted = jse.encrypt(password)
  return encrypted
}