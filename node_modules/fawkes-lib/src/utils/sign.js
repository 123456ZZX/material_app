/*
 * @Author: gao_m3
 * @Date: 2021-03-29 17:51:08
 * @LastEditors: wei_jt@ecidi.com
 * @LastEditTime: 2022-03-31 09:36:48
 * @Descripttion: 
 */
import HmacSHA1 from 'crypto-js/hmac-sha1'
import Base64 from 'crypto-js/enc-base64'
import { isEmpty } from './util'

const CLIENT = 'fawkes'//'dca84d27aeaa6783b6405fd222a2897a'//应用id
const CLIENT_SECRET = 'fawkes_secret'//'73af5c8eb55cd81e696f97ec8151cf5eafa62ce2659c0139ebda3dec41e18594'
const dayjs = require('dayjs')
//获取加密后的url参数字符串
const getUrl = (rest) => {
  let Params = ""
  let ts = dayjs().unix()
  let ttl = 30
  let obj = rest
  Params += "ts=" + ts + "&ttl=" + ttl + "&uid=" + CLIENT + (isEmpty(obj) ? '' : "&" + objTransUrlParams(obj))
  let ParamArr = sortUrlParams(Params)
  ParamArr = objKeySort(ParamArr)
  let paramstr = []
  for (let i in ParamArr) {
    paramstr.push(i + "=" + ParamArr[i])
  }
  paramstr = paramstr.join("&")
  let signWordArray = HmacSHA1(paramstr, CLIENT_SECRET)
  let sign = Base64.stringify(signWordArray)
  let encodeSign = encodeURIComponent(sign)
  return paramstr + "&sign=" + encodeSign
}

//获取加密后的参数对象
const getSign = (rest) => {
  let Params = ""
  let ts = dayjs().unix()
  let ttl = 180
  let obj = rest
  Params += "ts=" + ts + "&ttl=" + ttl + "&uid=" + CLIENT + (isEmpty(obj) ? '' : "&" + objTransUrlParams(obj))
  let ParamArr = sortUrlParams(Params)
  ParamArr = objKeySort(ParamArr)
  let paramstr = []
  for (let i in ParamArr) {
    paramstr.push(i + "=" + ParamArr[i])
  }
  paramstr = paramstr.join("&")
  let signWordArray = HmacSHA1(paramstr, CLIENT_SECRET)
  let sign = Base64.stringify(signWordArray)
  return { sign, ts, ttl, uid: CLIENT, ...ParamArr }
}

const objKeySort = (obj) => {
  let newkey = Object.keys(obj).sort()
  let newObj = {}
  for (let i = 0; i < newkey.length; i++) {
    newObj[newkey[i]] = obj[newkey[i]]
  }
  return newObj
}

const sortUrlParams = (str) => {
  if (typeof str !== 'string') {
    return {}
  }
  let paramObj = {}
  let paramArr = decodeURI(str).split('&')
  // let paramArr = str.split('&');
  for (let i = 0; i < paramArr.length; i++) {
    let tmp = paramArr[i].split('=')
    let key = tmp[0]
    let value = tmp[1] || ""
    //if (typeof value === 'string' && isNaN(Number(value)) === false && value !== "") {
    //  value = Number(value);
    //}
    if (typeof paramObj[key] === 'undefined') {
      paramObj[key] = value
    } else {
      let newValue = Array.isArray(paramObj[key]) ? paramObj[key] : [
        paramObj[key]
      ]
      newValue.push(value)
      paramObj[key] = newValue
    }
  }
  return paramObj
}

const objTransUrlParams = (obj) => {
  const params = []
  Object.keys(obj).forEach((key) => {
    let value = obj[key]
    if (typeof value === 'undefined') {
      value = ''
    }
    params.push([key, value].join('='))
  })
  return params.join('&')
}

export {
  getUrl,
  getSign
}
