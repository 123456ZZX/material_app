"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.installDependencies = exports.hasNpm = void 0;
const execa_1 = __importDefault(require("execa"));
const logger_1 = require("./logger");
const child_process_1 = require("child_process");
let hasNpmCache;
function hasNpm() {
    if (hasNpmCache === undefined) {
        try {
            (0, child_process_1.execSync)('npm --version', { stdio: 'ignore' });
            hasNpmCache = true;
        }
        catch (e) {
            hasNpmCache = false;
        }
    }
    return hasNpmCache;
}
exports.hasNpm = hasNpm;
async function installDependencies() {
    logger_1.consola.info('Install Dependencies\n');
    try {
        const manager = hasNpm() ? 'npm' : 'yarn';
        await (0, execa_1.default)(manager, ['install', '--prod=false'], {
            stdio: 'inherit',
        });
        console.log('');
    }
    catch (err) {
        console.log(err);
        throw err;
    }
}
exports.installDependencies = installDependencies;
